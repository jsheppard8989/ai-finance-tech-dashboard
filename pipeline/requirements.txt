# Python dependencies for AI Finance Tech Pipeline
# Install with: pip install -r requirements.txt

# Email processing
imaplib2>=3.6

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Database
# NOTE: sqlite3 ships with the Python standard library — no PyPI package required.

# Web requests
requests>=2.31.0
beautifulsoup4>=4.12.0

# Environment variables
python-dotenv>=1.0.0

# Optional: For transcription (if using OpenAI API)
# openai>=1.0.0

# Local fast transcription (recommended for podcasts - no API, fewer errors)
faster-whisper>=1.0.0

# Optional: Stage A transcript digest (Gemini / Anthropic / OpenAI)
# anthropic>=0.34.0

# Optional: For enhanced podcast processing
# pydub>=0.25.0
# speechrecognition>=3.10.0