# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# Database
dashboard.db
*.db-journal

# Environment (CRITICAL: Never commit .env files!)
.env
.env.local
.env.*.local
.venv
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Large files (don't commit audio/transcripts to git)
audio/
whisper_queue/
whisper_done/
pipeline/audio/
pipeline/whisper_queue/
pipeline/whisper_done/
pipeline/transcripts/*.txt
!pipeline/transcripts/.gitkeep

# Logs
*.log
pipeline/logs/

# Pipeline state (lock files, etc.)
pipeline/state/auto_pipeline.lock
pipeline/state/last_evening_run.txt

# Local testing
.github_username

# Openclaw & sensitive (do not commit to public repo)
AGENTS.md
SOUL.md
USER.md
IDENTITY.md
MEMORY.md
HEARTBEAT.md
TOOLS.md
memory/
bip39.txt
pending_contacts.json

# Pipeline state & inbox (hide from explorer; keep in local git if needed)
pipeline/state/
pipeline/inbox/

# Generated site data (JSON/JS exported from pipeline)
site/data/